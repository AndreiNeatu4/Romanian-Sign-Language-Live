fastapi>=0.100.0
uvicorn[standard]>=0.23.0
mediapipe==0.10.14
numpy>=1.24.0
opencv-python-headless>=4.8.0
python-multipart>=0.0.6

# CUDA 12.1 build of PyTorch so the RTX 4080 accelerates the BiLSTM model.
# The wheel bundles the CUDA runtime + cuDNN, so no CUDA base image is needed —
# only the host NVIDIA driver + nvidia-container-toolkit (see docker-compose.yml).
# Falls back to CPU automatically if no GPU is exposed to the container.
# extra-index-url keeps PyPI as primary so all other packages still resolve.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.0
