# Docker requirements — torch and torchvision are already installed in the
# base image (pytorch/pytorch:2.3.1-cuda12.1-cudnn8-runtime), so they are
# intentionally omitted here.
#
# opencv-python-headless is used instead of opencv-python because containers
# run without a display; all training/extraction scripts work fine headless.

opencv-python-headless>=4.8.0
mediapipe>=0.10.14
numpy>=1.24.0,<2.0
tqdm>=4.65.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
albumentations>=1.3.0
onnx>=1.14.0
